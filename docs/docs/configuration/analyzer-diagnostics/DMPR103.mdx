# DMPR103 — Source mapping is incomplete

A mapping using `MappingCompleteness.Source` or `MappingCompleteness.Both` has a readable source property or field that is neither consumed nor explicitly ignored.

Bind the member, consume it by convention or a computed-member method, or add a method-scoped `IgnoreSourceMember` with an optional reason.
