# DMPR104 — Mapping completeness is disabled

The mapping explicitly uses `MappingCompleteness.None`. This warning makes the compatibility escape hatch visible in review and suppression policy.

Prefer `Target`, `Source`, or `Both`, and use typed member ignores for intentional omissions.
