# DMPR102 — Mapping configuration is invalid

An explicit binding, ignore, null policy, condition, computed member, hook, allow-list, or depth policy is stale, duplicated, ambiguous, or incompatible with the named mapping method.

Use `nameof` for member and mapping-method names, remove duplicate declarations, and ensure helper methods are static, non-generic, and use supported typed parameters.
